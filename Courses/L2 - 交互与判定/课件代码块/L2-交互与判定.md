# 第2课课件代码块 · 交互与判定

> **用途**：PPT 直接取用的代码块 + 讲师讲解要点
> **对应大纲**：第2课时「交互与判定」（开场钩子：怎么让宝石能点？）
> **代码来源**：教学版 v3.2 注释版（`scripts/main.gd` / `scripts/board.gd` / `scripts/gem.gd`）
> **核心内容**：点选交互（状态机 T1/T4）+ 三消判定（find_matches）+ 坐标换算
> **⚠️ 实操调整（8/16 定案）**：`_calc_score` 实操**移至第 3 课**（计分绑定消除/连锁，主题更贴）；本课实操 = debug print（块1 末尾）

---

## 代码块清单（PPT 可拆页）

| 块 | 内容 | PPT 用途 | 页面序列 |
|----|------|----------|----------|
| 块1 | 输入处理（_unhandled_input + world_to_grid + get_gem_at） | 讲动作输入 + 坐标换算 | `L2-输入处理.md` + 实操 |
| 块2 | 状态机 + 点选交互（State 枚举 + _on_pointer_down） | **核心**：交互规则表 T4 + 状态切换 T1 | `L2-状态机样板.md` |
| 块3 | 选中辅助 + 交换流程（_select/_deselect/_do_swap） | 调用链展开（跨三文件） | `L2-状态机样板.md` 页6 |
| 块4 | 匹配判定（find_match_groups/find_matches + try_swap） | **核心**：run 算法 + 去重 + 回滚 | `L2-三消判定样板.md` |
| 块5 | 消除计分（_calc_score）——**第 3 课实操题** | 预告：人生第一个函数 | L3（待产出） |

---

## 块1 · 输入处理（main.gd ⑤ + board.gd ④）

### 1a · 输入入口 _unhandled_input（main.gd）

```gdscript
## 输入处理：所有没被 UI 控件消费的输入都会进这个函数
## event.is_action_pressed("click") = InputMap 里名为 click 的动作（鼠标左键）
## 用「动作名」判断而不是「具体按键」：以后想改成触摸/键盘，只需改配置，不用改代码
func _unhandled_input(event: InputEvent) -> void:
	# 动画播放中（SWAPPING/RESOLVING）直接忽略输入——状态锁，防止连点
	if state == State.SWAPPING or state == State.RESOLVING:
		return
	if event.is_action_pressed("click"):
		await _on_pointer_down()
```

**讲师讲什么：**
- `_unhandled_input` = 所有没被 UI 抢走的输入都进这里（按钮点击会被 UI 先吃掉，不进来）
- **InputMap 动作**：`"click"` 是动作名（默认绑鼠标左键），不是按键——代码不认按键
- **状态锁**：SWAPPING/RESOLVING 时直接 return（动画期间不能点，防连点 bug）

### 1b · 坐标换算 world_to_grid / get_gem_at（board.gd ④）

```gdscript
## 世界坐标 → 网格坐标：像素位置落在第几格；点在棋盘外返回 (-1,-1)
func world_to_grid(world: Vector2) -> Vector2i:
	var local := world - origin              # 相对棋盘左上角的偏移
	var col := int(floor(local.x / CELL_SIZE))
	var row := int(floor(local.y / CELL_SIZE))
	if col < 0 or col >= GRID_SIZE or row < 0 or row >= GRID_SIZE:
		return Vector2i(-1, -1)              # 越界标记
	return Vector2i(col, row)

## 取指定格的宝石；越界、空盘或空位都返回 null
func get_gem_at(pos: Vector2i) -> Gem:
	if grid.is_empty():
		return null
	if pos.x < 0 or pos.x >= GRID_SIZE or pos.y < 0 or pos.y >= GRID_SIZE:
		return null
	return grid[pos.x][pos.y] as Gem
```

**讲师讲什么：**
- 坐标翻译三步：减 origin（棋盘居中偏移）→ 除以 64（格宽）→ floor 向下取整
- **floor 不是 int**：`int(-0.5)` 截断成 0（错），`floor(-0.5)` 向下取整得 -1（对）——负数场景必须 floor
- `(-1,-1)` 越界暗号 → `get_gem_at` 返回 null → 触发"点空白取消"分支
- 组合拳：`board.get_gem_at(board.world_to_grid(world))` 从里往外读

### 1c · 实操：debug print（main.gd ⑥ 开头 3 行改造）

```gdscript
func _on_pointer_down() -> void:
	var world := get_global_mouse_position()
	var grid_pos := board.world_to_grid(world)       # ① 翻译结果存进变量
	print("点击了格子：", grid_pos)                  # ② 打印它（新加的一行）
	var clicked: Gem = board.get_gem_at(grid_pos)    # ③ 再用变量取宝石（结果一样）
```

**讲师讲什么：**
- 原组合拳一行拆三行 + 一行 print——游戏行为不变，只是"开口说话"
- 输出面板：编辑器底部「输出」（F8）
- 验证：点宝石打印 (col,row)；点棋盘外打印 (-1,-1)
- 课后作业：打印 `clicked.type`（颜色编号）/ `state`（状态），观察状态机切换

---

## 块2 · 状态机 + 点选交互（main.gd ①⑥）⭐

### 2a · 状态枚举 State（main.gd ①）

```gdscript
## 游戏状态机：任何时刻游戏都处于其中一个状态（同一时间只有一个）
## IDLE      = 空闲，等待玩家点击（标题界面和对局中都可能处于它）
## SELECTED  = 已选中一颗宝石，等待下一步（交换/取消/换选）
## SWAPPING  = 交换动画播放中 → 锁输入，防止连点出 bug
## RESOLVING = 消除/下落动画播放中 → 锁输入
enum State { IDLE, SELECTED, SWAPPING, RESOLVING }
```

**讲师讲什么：**
- 状态机 = 游戏任何时刻都在四种状态之一（**同一时间只有一个**）
- 前两个是"玩家可操作"状态，后两个是"动画中锁输入"状态（刹车）
- 模板 T1：状态切换表可背——IDLE↔SELECTED 是今天的主角，SWAPPING/RESOLVING 是第 3 课的主角

### 2b · 点选交互 _on_pointer_down（main.gd ⑥）⭐

```gdscript
## 点选交互（核心规则，课件"交互规则表"）：
## - IDLE 点击宝石      → 选中它（进入 SELECTED）
## - SELECTED 点自己    → 取消选中（回 IDLE）
## - SELECTED 点相邻    → 交换两颗（核心玩法）
## - SELECTED 点非相邻  → 换成选那颗
## - 点击空白           → 取消选中
func _on_pointer_down() -> void:
	var world := get_global_mouse_position()                     # 鼠标的屏幕坐标
	var clicked: Gem = board.get_gem_at(board.world_to_grid(world))  # 坐标换算→取那颗宝石
	if clicked == null:                                          # 点到空白（棋盘外）
		_deselect()
		state = State.IDLE
		return
	if state == State.SELECTED and selected != null:
		if clicked == selected:                                  # ① 点自己 → 取消选中
			_deselect()
			state = State.IDLE
			$SfxClick.play()
			return
		if board.is_adjacent(selected, clicked):                 # ② 点相邻 → 交换（相邻判断由 Board 负责）
			await _do_swap(selected, clicked)
			return
		_deselect()                                              # ③ 点非相邻 → 换成选它
		_select(clicked)
		$SfxSelect.play()
		return
	_select(clicked)                                             # 空闲状态点宝石 → 选中
	state = State.SELECTED
	$SfxSelect.play()
```

**讲师讲什么：**
- **规则表先行**（交互规则表 T4）：5 个分支 = 规则表 5 行，代码就是表的翻译
- 关键调用：`board.is_adjacent(selected, clicked)`——"相邻"由 Board 说了算（黑盒：Main 不问为什么，只问是不是）
- **画面与数据同步**：`_deselect()`（灭面具 + 清 selected）+ `state = State.IDLE` 永远成对出现
- 音效点缀：点自己 click / 选中 select / 交换 swap——让反馈更清晰

---

## 块3 · 选中辅助 + 交换流程（main.gd ⑦⑧）

### 3a · 前置：gem.gd 的 set_selected（L1 伏笔兑现，3 行）

```gdscript
## 选中反馈：true 亮遮罩 / false 熄遮罩（gem.gd ③）
func set_selected(on: bool) -> void:
	_select_mask.visible = on
```

**讲师讲什么：** L1 埋的伏笔——SelectMask 那个藏起来的面具，开关就是这 3 行。**本课完整讲它**：先认识函数（3 行），再讲调用时机（下面 `_select`/`_deselect` 里）。

### 3b · 选中辅助 + 交换流程

```gdscript
## 选中：记录引用 + 点亮遮罩（视觉反馈）
func _select(gem: Gem) -> void:
	selected = gem
	if selected != null:
		selected.set_selected(true)      # L1 学的面具亮，在这被调用！

## 取消选中：熄灭遮罩 + 清空记录
func _deselect() -> void:
	if selected != null and is_instance_valid(selected):
		selected.set_selected(false)
	selected = null

## 交换流程（只管流程，规则判定在 Board.try_swap）：
## 1. 状态切 SWAPPING（锁输入）
## 2. 让 Board 尝试交换（播放动画 + 判定是否有匹配）
## 3. 成功 → 切 RESOLVING，等 Board 结算完（消除/下落/连锁）→ 回 IDLE
## 4. 失败（交换后无匹配）→ 直接回 IDLE（Board 已自动弹回原位）
func _do_swap(a: Gem, b: Gem) -> void:
	state = State.SWAPPING
	_deselect()
	$SfxSwap.play()
	var ok: bool = await board.try_swap(a, b)
	if not ok:
		state = State.IDLE
		return
	state = State.RESOLVING
	await board.resolve_board()
	state = State.IDLE
```

**讲师讲什么：**
- `_select/_deselect` 是"面具开关"的调用方——**3a 的 `set_selected` 在这被真正调用**（伏笔兑现）
- `_do_swap` 十行代码**没有任何规则**——只做四件事：锁状态 → 问 Board → 等结算 → 解锁
- 调用链（跨三文件）：`_do_swap` → `board.try_swap` → `board._animate_swap_pair`（Tween 动画，📍 第 3 课 T3）/ `_swap_grid_data`（数据交换，见 4d）
- `await` 的含义：等 Board 干完活再继续（第 3 课 T3 模板详讲动画串行）
- 黑盒法：学员只需认识 `try_swap(a, b) -> bool`（true=成了 / false=弹回）

---

## 块4 · 匹配判定（board.gd ⑥）⭐

### 4a · find_match_groups（核心算法）

```gdscript
## 找出所有匹配组：每组是一段连续 ≥3 个同色宝石的列表
## 算法思路（伪代码版）：
##   逐行扫描：找到一段连续同色 run，长度 ≥3 就收进结果
##   逐列扫描：同上
##   返回：所有组的数组（一组 = 一个宝石数组）
func find_match_groups() -> Array:
	var groups: Array = []
	# —— 横向（逐行扫）——
	for row in range(GRID_SIZE):
		var run_start := 0
		while run_start < GRID_SIZE:
			var gem0: Gem = grid[run_start][row] as Gem
			if gem0 == null:                # 空位跳过
				run_start += 1
				continue
			var run_type: int = gem0.type   # 这段连续段的颜色
			var run_end := run_start + 1
			while run_end < GRID_SIZE:      # 向右延伸，直到颜色不同或越界
				var g: Gem = grid[run_end][row] as Gem
				if g == null or g.type != run_type:
					break
				run_end += 1
			if run_end - run_start >= 3:    # 连续 ≥3 才算一组
				var group: Array = []
				for c in range(run_start, run_end):
					group.append(grid[c][row])
				groups.append(group)
			run_start = run_end             # 从这段之后继续找下一段
	# —— 纵向（逐列扫，逻辑同上，只是 col/row 互换）——
	for col in range(GRID_SIZE):
		var run_start := 0
		while run_start < GRID_SIZE:
			var gem0: Gem = grid[col][run_start] as Gem
			if gem0 == null:
				run_start += 1
				continue
			var run_type: int = gem0.type
			var run_end := run_start + 1
			while run_end < GRID_SIZE:
				var g: Gem = grid[col][run_end] as Gem
				if g == null or g.type != run_type:
					break
				run_end += 1
			if run_end - run_start >= 3:
				var group: Array = []
				for r in range(run_start, run_end):
					group.append(grid[col][r])
				groups.append(group)
			run_start = run_end
	return groups
```

**讲师讲什么：**
- **核心概念 run**：一段连续同色 = run；算法 = 逐行找 run + 逐列找 run，≥3 收组
- 四个变量一台戏：`run_start` 起点 / `run_end` 终点 / `run_type` 颜色 / 长度 = run_end - run_start
- **起点跳到 run_end 不回头**：每格只扫一次（效率 + 不漏）
- 空位（null）跳过——下落过程中棋盘有空位，扫到直接跳
- 纵向代码不用看：逻辑一模一样，col/row 互换（PPT 备注放完整版）

### 4b · find_matches + _add_unique（去重）

```gdscript
## 把匹配组"摊平"成一个宝石数组（一颗宝石可能同时属于横/纵两组，需去重）
func find_matches() -> Array:
	var matched: Array = []
	var groups := find_match_groups()
	for group in groups:
		for gem in group:
			_add_unique(matched, gem as Gem)
	return matched

## 朴素去重：gem 不在数组里才追加（避免同一颗被横纵两组重复计算）
func _add_unique(arr: Array, gem: Gem) -> void:
	if gem == null:
		return
	if arr.has(gem):
		return
	arr.append(gem)
```

**讲师讲什么：**
- **L 形陷阱**：横组 + 竖组共用中间那颗——不去重会消除两次、计分翻倍
- `_add_unique` = "收过就跳过"（arr.has(gem) 检查）

### 4c · try_swap（交换合法性：先换 → 检查 → 不行回滚）

```gdscript
## 尝试交换（对外接口③）：合法且有匹配返回 true；无匹配自动弹回返回 false
func try_swap(a: Gem, b: Gem) -> bool:
	if a == null or b == null:
		return false
	if not is_adjacent(a, b):
		return false
	_swap_grid_data(a, b)              # ① 数据交换
	await _animate_swap_pair(a, b)     # ② 播放交换动画
	var matches := find_matches()      # ③ 检查交换后有没有匹配（今天学的判定）
	if matches.is_empty():
		_swap_grid_data(a, b)          # ④ 没有 → 换回去（弹回）
		await _animate_swap_pair(a, b)
		return false
	return true                        # ⑤ 有匹配 → 交换成立
```

**讲师讲什么：**
- **回滚套路**（游戏开发常用思想）：先做 → 检查 → 不行就回滚——交换失败弹回就是这个
- 数据与表现分离：`_swap_grid_data`（grid 数组）先换，`_animate_swap_pair`（屏幕动画）后播（代码见 4d）
- 相邻判断（`is_adjacent`）完整代码见 4d——"相邻"由 Board 定义

### 4d · try_swap 的内部函数（按「调用链不黑盒」原则展开）

**① is_adjacent —— 相邻判断（L2 知识点）**

```gdscript
## 两颗宝石是否上下左右相邻？曼哈顿距离 == 1（横差+纵差为 1）
## 公开给 main.gd 调用：网格的"相邻"由 Board 说了算
func is_adjacent(a: Gem, b: Gem) -> bool:
	var dx := absi(a.grid_pos.x - b.grid_pos.x)
	var dy := absi(a.grid_pos.y - b.grid_pos.y)
	return dx + dy == 1
```

**讲师讲什么：**
- 曼哈顿距离 = 横差 + 纵差；`== 1` = 上下左右相邻（斜对角是 2，不算）
- `absi` = 绝对值——⚠️ 常见坑：Godot 4 警告当错误，`abs()` 编不过要用 `absi`

**② _swap_grid_data —— 只换数据（不碰屏幕）**

```gdscript
## 只交换"数据"（grid 数组里的引用 + 各自的 grid_pos），不动屏幕位置
## 屏幕动画由 _animate_swap_pair 单独负责——数据与表现分离
func _swap_grid_data(a: Gem, b: Gem) -> void:
	var pos_a := a.grid_pos
	var pos_b := b.grid_pos
	grid[pos_a.x][pos_a.y] = b
	grid[pos_b.x][pos_b.y] = a
	a.grid_pos = pos_b
	b.grid_pos = pos_a
```

**讲师讲什么：**
- **数据与表现分离**：先换 grid 数组（数据），动画后播（表现）——弹回时也是先换数据再播动画
- grid 是棋盘"真相"，宝石的 `grid_pos` 必须跟着数据走（两边一致原则）

**③ _animate_swap_pair —— 交换动画（📍 第 3 课 T3 模板，今天带过）**

```gdscript
## 两颗宝石并行滑到各自新位置（一颗 Tween 同时动画两行）
func _animate_swap_pair(a: Gem, b: Gem) -> void:
	var tween := create_tween()
	tween.set_parallel(true)                                       # 并行模式
	tween.tween_property(a, "position", grid_to_world(a.grid_pos), 0.15)
	tween.tween_property(b, "position", grid_to_world(b.grid_pos), 0.15)
	await tween.finished                                           # 等动画播完（T3：动画必须 await 串行）
```

**讲师讲什么（1 分钟带过，不精讲）：**
- Tween 动画 + `await tween.finished`（串行模板 T3）是**第 3 课内容**——今天只需知道：try_swap 会等动画播完再继续
- 代码先贴在这（PPT 备注可放），学员课后自己看

---

## 块5 · 消除计分 _calc_score（第 3 课实操题 · 预告）

```gdscript
## 计分规则：每组 3 个 = 30 分，每多 1 个 +10（4 连 = 40，5 连 = 50…）
## 这是第 3 课实操题：学员要自己重写这个函数体（人生第一个函数）
func _calc_score(groups: Array) -> int:
	# TODO（第 3 课学员填写）
	pass
```

**讲师讲什么（预告 1 分钟）：**
- 今天学完"判定"（找出匹配组），第 3 课学"消除"（把匹配组消掉）——计分就发生在那
- 预告：`_calc_score` 是**你们要亲手写的第一个完整函数**，输入匹配组、输出分数
- 连锁倍率（第 n 次消除 ×n）也在这——所以计分放第 3 课讲更顺（8/16 调整）

---

## 涉及文件与代码地图

| 文件 | 功能分区 | 本课用到 |
|------|---------|----------|
| main.gd | ①状态枚举 ⑤输入 ⑥点选交互 ⑦选中辅助 ⑧交换流程 | 块1/块2/块3 |
| board.gd | ④坐标换算 ⑥匹配判定 ⑧交换（is_adjacent/try_swap） | 块1/块4 |
| gem.gd | ③选中反馈（set_selected） | 被调用（L1 已讲） |

---

*v1：2026-08-16 | 小龙虾 🦞 · L2 代码块（实操调整：_calc_score → 第3课）*
