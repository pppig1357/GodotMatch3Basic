# 第3课 · 讲师逐行讲稿 v1

> **用途**：PPT 放代码，讲师按「人话」栏念
> **配套**：代码块文档 `L3-游戏循环.md`（PPT 取代码用）+ 页面序列 `L3-结算循环样板.md` / `L3-动画下落样板.md`
> **核心比喻**：**连锁 = 循环**（while 有匹配就继续消）；**await = 动画的刹车**；**movers = 记账本**（先记账后兑现）

---

## 块1 · Tween 组件与 animate_clear（gem.gd ④，模板 T3）

> PPT 显示 animate_clear + Tween 速查（见 L3-游戏循环.md 块1），配合《L3-拆页文案-消除下落连锁.md》A1~A2 使用
> ⚠️ 2026-09-09 定案：`animate_swap_to` / `animate_fall_to` 已从 gem.gd 删除（死代码），**gem.gd 只剩 animate_clear**

| 代码 | 人话（照读） |
|---|---|
| `var tween := create_tween()` | 「**Tween = 动画调度器**：Godot 的动画工具。补间动画（inbetweening）——你给起点/终点/时长，中间帧电脑算。」 |
| `tween.set_parallel(true)` | 「并行模式：后面的属性**同时**播。缩小和淡出一起进行——注意这是『一个动画内部』的并行，跟『动画之间串行』不冲突。」 |
| `tween.tween_property(self, "scale", Vector2(0.2, 0.2), 0.2)` | 「把缩放 scale 在 0.2 秒内变成 0.2 倍——'缩小消失'的一半。」 |
| `tween.tween_property(self, "modulate:a", 0.0, 0.2)` | 「modulate:a = 透明度通道，0.0 = 完全透明——配合缩小，就是'缩小消失'效果。」 |
| `await tween.finished` | 「**T3 模板，全课最该背的一句**：等动画播完才继续。不写它，动画播到一半下一段就开始了——宝石会乱飞。L2 交换动画（`_animate_swap_pair`）也 await 过它，今天正式命名。」 |

**口播补充（Tween 概念，配合 A1/A2 页）：**
- 「Tween 来自 inbetweening（中间画）：你只写关键帧，电脑补中间帧。我们所有动画（交换/消除/下落）都是它。」
- 「为什么不用 AnimationPlayer？它的动画要在编辑器里提前'做'好；我们的宝石落几格、移多远是**对局中算出来**的，必须代码生成——用 Tween。」

**板书建议：** 写 T3 模板大字：`await tween.finished`，旁边画时间轴（动画条 → await 停顿 → 下一段）。

| 代码 | 人话（照读） |
|---|---|
| `var chain := 1` | 「连锁计数器，从 1 开始。第 1 次消除 ×1，第 2 次 ×2——记住这个数，等会儿计分要用。」 |
| `while true:` | 「无限循环——只要不主动退出就一直转。为什么不用『有匹配才进』？因为我们要**先检查再决定**，这更安全。」 |
| `var matches: Array = find_matches()` | 「每次循环先问：现在还有没有匹配？——第 2 课学的判定，在这被反复调用。」 |
| `if matches.is_empty(): break` | 「没了？**break 跳出循环**，结算结束。这是唯一的出口。」 |
| `await clear_matches(matches, chain)` | 「有匹配：消除 + 计分。await = 等消除动画播完才继续。」 |
| `await fall_and_refill()` | 「然后下落 + 补新。也是等动画播完。」 |
| `chain += 1` | 「连锁次数 +1，回循环开头再检查——**这就是连锁**：消完可能又凑出新三消。」 |

**板书建议：** 画循环图：`检查 → 消除 → 下落 → 回检查`，标"chain 每次 +1"。

---

## 块3b · _calc_score（实操：人生第一个函数）

| 口播 |
|------|
| 「现在写人生第一个函数。需求：每组 3 个 = 30 分，多 1 个 +10。函数签名我写好了，你们填函数体。三个零件：**for 遍历每组**、**group.size() 数个数**、**total += 累加**。条件判断：个数 ≥3 才算分。」 |
| 「讲师先完整写一遍——`var total := 0` 存总分；`for group in groups:` 一组一组来；`if n >= 3:` 够 3 个才加分；`total += 30 + (n - 3) * 10` 公式；最后 `return total`。」 |
| 「验证：消除 3 连 = 30 分，4 连 = 40 分。写不出来就对照我写的抄一遍，抄完改成自己的——**抄也是一种学习**。」 |

**防手抖速查：**
- 忘写 `return total` → 报错（函数声明了 `-> int` 必须返回）
- `group.size()` 写成 `group.size` → 报错（少括号）
- 空数组传入 → 循环不执行，返回 0（正常，不报错）

---

## 块4 · fall_and_refill（下落填充，伪代码先行）

| 代码 | 人话（照读） |
|---|---|
| `var movers: Array = []` | 「先开个**记账本**：每颗要动的宝石记一行——谁、落到哪、花多久。先记账，最后统一播动画。」 |
| `for row in range(GRID_SIZE - 1, -1, -1)` | 「自下而上扫——**必须从底部往上**，不然宝石会"卡"在半路。像往杯子里倒沙子，从底下压实。」 |
| `grid[col][row] = null; grid[col][write_row] = gem` | 「数据搬移：原位置清空、新位置填入。画面还没动，**数据先动**——第 2 课的老套路：数据与表现分离。」 |
| `maxf(FALL_PER_CELL, float(cells) * FALL_PER_CELL)` | 「时长按格数算：落 1 格 0.08 秒，落 3 格 0.24 秒——**落下越快，视觉越自然**（恒速）。」 |
| `var start_pos := Vector2i(col, -1 - spawn_index)` | 「新宝石从屏幕外上方（行 -1、-2…）排队落入——像补货机往货架上补货。」 |
| `tween.set_parallel(true); for item in movers: ...` | 「记账本一次性兑现：一个并行 Tween 同时播放所有下落动画，await 一次搞定。」 |

**板书建议：** 画一列宝石下落示意：空洞在下 → 上面宝石压实 → 顶部补新（从 -1 行落入）。

---

## 块5 · 实操引导（_calc_score，配合实操模板）

| 口播 |
|------|
| 「翻开实操模板 `L3-calc-score.md`——函数签名、注释我都写好了，函数体留空。先听我讲一遍函数概念（10 分钟），然后看我完整写一遍，最后轮到你们。」 |
| 「函数概念就一句话：**输入 → 处理 → 输出**。输入是 groups（匹配组数组），处理是遍历套公式，输出是 return 总分。就像榨汁机：水果进去（输入），果汁出来（输出）。」 |
| 「写的时候三个零件：**for 遍历**、**size 数数**、**+= 累加**。写完 F5 验证：3 连 = 30 分、4 连 = 40 分。写不出来？先抄我的，抄完改成自己的——**抄也是一种学习**。」 |

**防手抖速查（巡视用）：**
- 忘写 `return total` → 报错（`-> int` 必须返回）
- `group.size()` 写成 `group.size` → 报错（少括号）
- `if n >= 3` 写成 `n > 3` → 4 连少算（3 个的组被跳过）
- 空数组传入 → 返回 0（正常）
- 学员卡住超 5 分钟 → 让他对照旁边同学的抄一遍再改

---

*v1：2026-08-16 | 小龙虾 🦞 · 补块1 动画 T3 完整口播 + 实操引导*
