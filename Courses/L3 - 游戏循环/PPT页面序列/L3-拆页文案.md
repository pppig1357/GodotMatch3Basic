# L3 · 拆页文案 —— 总纲与索引（已按主题拆分）

> **说明（2026-09-09 v3）**：本文件是 L3 拆页文案的**总纲与索引**，详细拆页按**洋葱式**（对齐 L2：伪代码地图先行 → 原料/零件 → 组装 → 总装）拆为两份：
> 1. **《L3-拆页文案-消除下落连锁.md》**（**13 页**）：R0 地图 → O1-3 开场 → A1-A2 动画零件（认识 Tween / animate_clear）→ **F 系列**（**F1 伪代码细化版**：细化步骤 + 一列宝石跑一遍 + 为什么拆 7 页 → F2-F7 按代码顺序细拆六段，每页配图解/口播，讲完即完整 fall_and_refill）
> 2. **《L3-拆页文案-结算计分.md》**（**16 页**）：**零件**（S1-S8 实操 _calc_score）→ **组装**（K1a/K1b clear_matches + K2/K3 关联）→ **总装**（C1/C2 resolve_board ⭐）→ 收尾（E1/E2）
>
> 全局信息（方法论 / 模板表 / 文件地图 / 调用链）保留在本文件；每份分文件的页序表见其头部。

---

> **⏱️ 教学节奏**：本课 120min——回顾讲评 10 → Tween/动画 20 → 下落（按代码顺序三段）25 → 实操 _calc_score 40 → 消除组装+循环总装 20 → 小结 5 + 缓冲 5
> **方法论（对齐 L1/L2）**：**伪代码前置 + 洋葱式拆解**——每个单元先讲思想再对代码；动画/循环/下落都是"先记账、后兑现"套路
> **⚠️ 页面硬约束**：① 一页 PPT 代码 ≤15 行（fall_and_refill 全貌 40 行作"供阅读"页，字号降档，不逐行讲）② PPT 代码块去注释（口播现场讲）③ 底部安全边距（生成 PPT 时校验）
> **配套**：`课件代码块/L3-游戏循环.md`（块1~块4）+ `讲师讲稿/L3-逐行讲稿.md` + 实操模板 `Courses/实操模板/L3-calc-score.md`
> **衔接**：前接 L2（交互与判定：`try_swap` 交换 → 本课 `resolve_board` 让循环转起来）；后接 L4（信号流全景 + 打包）
> **🔮 黑盒兑现（本课核心叙事）**：L2 结束时 `try_swap` 成功后调用 `resolve_board` 是个黑盒——**本课把它拆开**：它就是"while 有匹配就消除→下落"的循环，连锁由此而来

**本课三个模板（可背清单，接 L2 的 T1/T4）：**
| 模板 | 内容 | 用在哪 |
|------|------|--------|
| **T2 结算循环** | `while 有匹配: 消除 → 下落`（连锁 = 循环重复） | 核心引擎 |
| **T3 动画串行** | `await tween.finished`（动画之间必须等） | 所有动画 |
| 记账本模式 | movers 数组先记账 → 一个并行 Tween 统一兑现 | 下落填充 |

---

## 📁 文件地图（L3 动两个文件 + 一个实操）

> 与 L2 不同，L3 的**新逻辑全在 board.gd**（main.gd 不用动！）——L2 的 `_do_swap` 已经调用了 `board.resolve_board()`，本课只是把它"拆开看"

| 文件 | 页 | 内容 | 角色 |
|---|---|---|---|
| 📁 地图 | R0 | 伪代码地图（完整游戏循环全景） | 全局地图 |
| 🟢 gem.gd | A1-A2 | Tween 前置：认识 Tween + animate_clear（T3 模板） | gem 零件① |
| 🟠 board.gd | F1-F7 | ⑪ 下落填充 fall_and_refill（**F1 伪代码细化版 + 按代码顺序细拆六段**：骨架/压实/记账/补新/播放） | 下落 ⭐ |
| ✍️ 实操 | S1-S8 | ⑩ `_calc_score` 函数概念 + 实操（人生第一个函数） | 计分零件② ⭐ |
| 🟠 board.gd | K1a-K3 | ⑩ 消除组装：clear_matches（拆两页）+ 倍率/状态锁 | 组装② ⭐ |
| 🟠 board.gd | C1-C2 | ⑨ 循环总装：resolve_board（T2 模板）⭐ | 总装 |
| 🏁 收尾 | E1-E2 | 小结 / L4 预告 | 收束 |---
---

## 🔗 调用链处理说明（对照全课原则）

- **被调函数**：`find_matches` / `find_match_groups`（L2 判定层）→ 只标"黑盒复用"，不重复展开；`_spawn_gem` / `random_gem_type`（L1）→ 一句话带过
- **跨文件**：gem.gd 唯一动画函数 `animate_clear`（④）在《消除下落连锁》A2 精讲（await 刹车 + 并行）；board.gd 是主角（⑨⑩⑪）
- **信号**：`match_cleared` / `score_changed` → 只说"发出去有人响应"，L4 信号流细讲（伏笔）
- **模板呼应**：T2/T3 是 L3 新模板（接 L2 的 T1/T4）；记账本模式是本课技巧
- **实操边界**：学员只写 `_calc_score` 函数体（模板见 `Courses/实操模板/L3-calc-score.md`）；`clear_matches` 调用方式讲师演示即可；改 `clear_matches` 是作业②

---

*v7：2026-09-09 | 小龙虾 🦞 · 文件1 13 页（F0 并入 F1：伪代码细化版）；文件2 16 页（S 计分零件 → K 消除组装 → C 循环总装 → E 收尾）*
