# GodotMatch3 开发记录

> Infoco 编程社 × 独游社 · 宝石迷阵三消社课演示项目  
> 引擎：Godot **4.7.1**（Steam）· 语言：GDScript  
> 文档用途：供策划/主讲调整课件大纲、对照官方教程、复盘实现决策  
> 整理日期：2026-08-12（基于本仓库从零到可玩成品的完整开发上下文；同日补记 v2.1 体验下沉）

---

## 0. 项目目标与分工回顾

| 角色 | 职责 |
|------|------|
| pppig | 策划、主讲、验收 |
| Cursor | 代码实现与迭代 |
| 小龙虾 | 技术骨架 / Prompt / 素材清单等文档 |

**课程定位：** 面向零基础大学生的 4 课时项目驱动课，每课时结束应有可运行成果。  
**参考对照：** Godot 官方「您的第一个 2D 游戏」（本地参考工程：`E:\Game soft\godot project\pppig-first-game`）。

**早期决策（见根目录 README / 技术骨架）：**

- 引擎选 Godot 4.7（轻量、GDScript 友好）
- 美术先零依赖占位（ColorRect），M4 再换 CC0 贴图
- 教学约束：注释中文、不用 autoload、避免 lambda/高阶技巧、单文件宜 ≤150 行（超了拆文件）

---

## 1. 文档与 Prompt 体系（开发前）

仓库初始几乎只有 Markdown，代码由 Cursor 按阶段 Prompt 落地：

| 文档 | 作用 |
|------|------|
| `技术骨架.md` | 场景树、数据结构、状态机、算法、教学约束、导出建议 |
| `课件大纲-4课时.md` | 4 课时知识点 ↔ 模块 ↔ 作业 ↔ 常见坑 |
| `cursor-prompts/M1~M4.md` | 分阶段给 Cursor 的实现说明 |
| `cursor-prompts/FULL-完整成品.md` | 一次性成品规格（含素材路径） |
| `cursor-prompts/归档/BASIC-v2.1-体验下沉补丁.md` | v2.1 体验下沉 Prompt（**已落地归档**，见第 7.5 节） |
| `官方教程对照.md` | 官方入门教程与本课覆盖对比 |
| `assets/素材清单.md` | CC0 素材来源、用法、Credits |

**可行性审阅结论（开发启动前）：** 在 Godot 4.7 下按 M1–M4 交付完整可玩三消 **可行**。需注意：

1. ColorRect 挂在 Node2D 下必须设 `size`
2. 动画用 `await tween.finished` 串行，避免乱并发 Tween
3. 实时 `AudioStreamGenerator` 不适合社课；后改为现成 ogg
4. Web 导出建议关 `thread_support`
5. `board.gd` 在 M3 后会膨胀 → 拆 `board_resolve.gd`

---

## 2. 里程碑总览

| 阶段 | 交付 | 验收要点 |
|------|------|----------|
| **M1** | 项目骨架 + 8×8 彩色网格 | 居中、可运行、开局无三消 |
| **M2** | 选中 / 交换 / 三消 / 计分 | 无效交换弹回不扣步；动画期间锁输入 |
| **M3** | 下落 / 填充 / 连锁 / 状态机 | 连锁倍率；步数曾为强制限步 |
| **M4** | HUD / 素材 / 音效 BGM / 导出 | 限步改为可选；对齐官方 HUD+音频写法 |
| **打磨** | 拖拽交换、BGM 更换、背景、半遮罩、延后生成 | 见第 7 节 |
| **v2.1** | 体验下沉补丁（教学主线） | 开局防死局、再点取消、亮色 HUD、拖拽正式化；见第 7.5 节 |

每阶段原则：**必须可 F5 运行**，不交付半成品。

---

## 3. M1 · 网格渲染（实现记录）

### 3.1 交付内容

- `project.godot`：640×720、Compatibility 渲染、`click`（鼠标左键）
- `scenes/main.tscn`：`Main → Board`
- `scripts/gem.gd` + 后改为 `scenes/gem.tscn` 场景实例化
- `scripts/board.gd`：二维数组、`grid_to_world` / `world_to_grid`、`find_matches`、`ensure_no_initial_matches`

### 3.2 关键实现细节

- **坐标：** `origin` 使 8×64=512 棋盘在视口居中；`grid_pos` 为 `(col, row)`
- **开局防自消：** 生成后循环 `find_matches`，对匹配格重随机颜色，带安全次数上限
- **宝石创建顺序：** 必须先 `add_child` 再 `setup`（`@onready` 依赖进树）
- **演进：** 先代码里 `ColorRect.new()`，后改为 `gem.tscn` + `preload` + `instantiate()`，对齐官方「场景即可复用类型」教学点

### 3.3 课件可强调的坑

- ColorRect 不设 `size` → 看不见
- 用 `position` 而非误用全局坐标导致错位
- 二维数组越界（0~7）

---

## 4. M2 · 交互与三消判定

### 4.1 交付内容

- `main.gd`：输入 + 状态雏形（IDLE / SELECTED / SWAPPING…）
- `board.gd`：`try_swap`、`find_match_groups`、`clear_matches`、计分、`score_changed`
- `gem.gd`：选中反馈、交换/消除 Tween
- ScoreLabel（后并入 HUD）

### 4.2 规则约定（课件必须写死）

- **仅「交换后产生匹配」才算成功**；无匹配动画弹回，**不扣步**
- 计分：每组 3 个 = 30，每多 1 个 +10
- 消除后 **留空位**（下落留给 M3）
- 动画期间禁止输入（状态锁）

### 4.3 选中反馈的迭代

| 版本 | 表现 | 结果 |
|------|------|------|
| 初版 | `scale` 放大 | 易挤占邻格，观感差 |
| 现行 | `SelectMask` 半透明白遮罩 | 保留；与拖拽并存 |

### 4.4 重要踩坑：Variant 推断当错误

项目开启「警告当作错误」后：

```text
The variable type is being inferred from a Variant value...
```

**原因：** `abs()` 返回 Variant；`$Board` 标成 `Node2D` 时动态调用也返回 Variant。  

**修复：**

- `abs` → `absi`
- `preload("board.gd")` 作脚本类型，或 `as Gem` 显式转换
- `instantiate() as Gem`

**课件建议：** 单独加一小节「GDScript 类型与警告当错误」，否则学员复制 `abs` 会直接编不过。

---

## 5. M3 · 下落、连锁与状态机

### 5.1 拆分文件（教学点）

`board.gd` 在 M2 末已偏长，M3 按骨架拆出：

| 文件 | 职责 |
|------|------|
| `board.gd` | 网格、匹配、交换判定、扣步、信号 |
| `board_resolve.gd` | `RefCounted` 辅助类：`resolve_board` / `clear_matches(chain)` / `fall_and_refill` |

Board 持有 `_resolve = ResolveHelper.new(self)`，Tween 建在 Board 节点上（`board.create_tween()`），便于一次 `await`。

### 5.2 流程

```
try_swap（仅判定+可选扣步）
  → 成功则 main 进入 RESOLVING
  → resolve_board：while 有匹配 → clear(×chain) → fall_and_refill
  → 回到 IDLE；若限步且步数为 0 → GAME_OVER
```

- **下落：** 逐列自下而上压实；顶部从网格上方生成再落入  
- **动画：** 同阶段多颗并行 Tween，阶段之间串行  
- **连锁倍率：** 第 n 次消除分数 ×n  
- **下落时长：** `FALL_PER_CELL = 0.08`，按格数近似恒速  

### 5.3 步数策略的变更（对课件很重要）

| 时期 | 策略 |
|------|------|
| M3 初版 | 强制 20 步，用尽 GAME_OVER |
| M4 起 | **限步为可选**；默认无限步，MovesLabel 显示 `∞` |

成功交换才扣步的约定不变；仅在 `moves_limited == true` 时扣步并可能结算。

---

## 6. M4 · UI、素材、音频、导出

### 6.1 素材落地（相对原 M4 Prompt 的重大修正）

原 M4 Prompt 写「零外部资源 / 程序化音效」。仓库已备 CC0 素材后，改为：

| 类型 | 路径 / 用法 |
|------|-------------|
| 宝石 sheet | `assets/sprites/gems/gem_bomb_rainbow.png`（52×52，40 帧/行，24 行） |
| 六色行号 | BLUE0 / GREEN3 / ORANGE6 / PURPLE9 / RED12 / YELLOW18（每色第 1 行几何宝石，跳过心/环/彩虹） |
| 字体 | `assets/ui-kenney/Font/Kenney Future.ttf` |
| 音效 | Kenney UI Sounds：click / tap / switch |
| BGM | 见第 7 节更换历程 |

`gem.gd` 用静态共享 `SpriteFrames` 切帧，`AnimatedSprite2D` 播放；保留 `SelectMask`。

### 6.2 UI / 音频架构重构（对齐官方教程）

对照 `pppig-first-game`：

| 官方教程 | 本项目现行 |
|----------|------------|
| 独立 `hud.tscn` + `start_game` 信号 | `scenes/hud.tscn` + `scripts/hud.gd` |
| Main 上 `$Music` / `$DeathSound` | `$Music` + `$SfxClick/Select/Swap/Clear` |
| `$HUD.update_score()` | 同模式；另有 `update_moves` / `show_game_over` / `show_title` |

**已删除（为精简）：** `ui.gd`、`audio_player.gd`（中间层过多）。

开局流程：

```
HUD StartButton → start_game(limited)
  → Main.new_game(limited)
  → board.reset_game → fill_grid
  → hud.prepare_playing()
```

### 6.3 导出

- `export_presets.cfg`：Windows Desktop → `build/GodotMatch3.exe`；Web 关 `thread_support`
- README 含运行/导出说明与 Credits

### 6.4 导入注意

新加的 `.ogg` 若无 `.import`，`preload` 会报 unrecognized extension。需用编辑器打开工程或：

```text
godot --path <项目> --headless --import
```

---

## 7. 打磨迭代（M4 验收后）

按时间顺序列出，便于课件「进阶 / 作业」取材。

### 7.1 交互

1. **选中：** 放大 → 改为半透明白遮罩（`SelectMask`）  
2. **拖拽交换：**（v2.1 起正式纳入教学主线，见 7.5）  
   - 按下选中并拖；过半格阈值（`DRAG_SWAP_THRESHOLD ≈ 28`）向主轴方向与邻格交换  
   - 视觉跟随限制在一格内  
   - 仍支持「点相邻第二颗」点击交换  
   - **再点已选中 → 取消选中回 IDLE**（v2.1）  
3. **未达阈值松开：** 弹回格点，保持选中  

### 7.2 BGM 更换历程（最终以代码为准）

1. `assets/audio/bgm/bgm_main.ogg`（NES jingle）  
2. `jingles_PIZZI03.ogg`  
3. `jingles_SAX03.ogg`  
4. **现行：** `assets/bgm-kenney/House In a Forest Loop.ogg`（循环，`$Music`）

### 7.3 背景与 HUD 遮罩

| 层 | 做法 |
|----|------|
| 主层 | `Main/Background` 深石板灰蓝 `(0.12, 0.14, 0.18)`，避开宝石高饱和色 |
| 清屏色 | `project.godot` → `default_clear_color` 同深色 |
| HUD | 曾用顶/底实心浅色条 → **改为全屏半透明遮罩** `Mask`（约 alpha 0.72） |

半遮罩行为：

- **标题 / 结算：** 显示 Mask + 标题文案 + 开始/再来一局 + 限步勾选；隐藏分数条  
- **对局中：** 隐藏 Mask；显示分数、步数、重新开始  

### 7.4 延后生成宝石（重要体验修复）

**问题：** 标题界面下已能看见整盘宝石（`board._ready` 里 `fill_grid`）。  

**现行：**

- `_ready` 只算 `origin`，不生成  
- `new_game` → `reset_game` → `fill_grid`  
- 回标题 → `clear_board()` 清空子节点与 `grid`  
- `get_gem_at` 对空盘直接返回 `null`  

**课件建议：** 第 4 课或打磨环节讲「资源/节点的生命周期与开始按钮」；标题阶段不应暴露玩法盘面。

### 7.5 体验下沉补丁 v2.1（2026-08-12 · 已归档）

> 定位：把 M5 完整版已验证的「体验硬伤修复」下沉回教学版；**只修体验，不引入扩展**。  
> Prompt 原文已归档：`cursor-prompts/归档/BASIC-v2.1-体验下沉补丁.md`  
> **明确不做：** 特殊宝石 / 模式切换 / 存档 / 提示系统（见 `cursor-prompts/M5-后续优化.md`，本仓库不执行）。

#### 交付四项

| # | 改动 | 文件 | 说明 |
|---|------|------|------|
| 1 | 开局防死局 | `board.gd` | 新增 `has_moves()` / `ensure_has_moves()`；`reset_game` 在 `ensure_no_initial_matches` 之后调用 |
| 2 | 再点取消选中 | `main.gd` | SELECTED 下再点已选中宝石 → `_cancel_drag` + `_deselect` → IDLE（不再误入拖拽） |
| 3 | HUD 亮色 UI | `hud.tscn` 仅资源 | 主按钮亮黄深字、次要/CheckButton 亮蓝、`TitlePanel` 挂在 `Mask` 下随遮罩显隐；**未改** `hud.gd` 逻辑 |
| 4 | 拖拽正式化 | `main.gd` 注释 | 拖拽逻辑本已存在；补交互规则表教学注释（离散点选 vs 连续拖拽并存） |

#### 关键实现笔记

- **`has_moves()`：** 枚举右/下相邻对 → `_swap_grid_data` 模拟 → `find_matches` 验证 → 必须换回。教学点：匹配检测的逆向应用；与 M5 `find_hint_swap` 同族但基础版不实现提示。  
- **`ensure_has_moves()`：** 无解则 `fill_grid` + `ensure_no_initial_matches` 循环，安全上限 100，超出 `push_warning`。  
- **交互共存：** 新选中仍 `_start_drag_on_selected()`；「再点取消」分支直接 return，不启动拖拽；阈值仍 `DRAG_SWAP_THRESHOLD = 28`（不随 M5 放大）。  
- **HUD：** 锁定参数 640×720 / `CELL_SIZE=64`；对局分数条仍浅色字，无需 TopBar。

#### 验收

- Headless 启动通过；连续 `reset_game` 10 局 `has_moves()` 均为 true  
- 再点取消与拖拽/点选交换不冲突；限步、连锁、结算行为与补丁前一致  

#### 教学注释落点（学员可见）

| 教学点 | 位置 |
|--------|------|
| find_matches 逆向 / 可行步检测 | `board.gd` → `has_moves()` |
| 与 `ensure_no_initial_matches` 兄弟关系 | `board.gd` → `ensure_has_moves()` |
| 离散点选 vs 连续拖拽 | `main.gd` → `_on_pointer_down()` 上方规则表 |
| 再点取消选中 | `main.gd` → 取消分支行内注释 |

---

## 8. 现行架构快照（2026-08-12 · 含 v2.1）

### 8.1 场景树

```
Main (Node2D) [main.gd]
├── Background (ColorRect)          # 深色主背景
├── Board (Node2D) [board.gd]
│   └── Gem 实例 × N（开始后生成）
├── HUD (CanvasLayer) [hud.tscn]
│   ├── Mask (半透明全屏)
│   │   └── TitlePanel (NinePatchRect，亮黄底板；随 Mask 显隐)
│   ├── ScoreLabel / MovesLabel
│   ├── Message / StartButton / MovesLimitedCheck / RestartButton
├── Music (AudioStreamPlayer)
├── SfxClick / SfxSelect / SfxSwap / SfxClear
```

### 8.2 脚本职责

| 脚本 | 职责 |
|------|------|
| `main.gd` | 状态机、点击/拖拽输入、再点取消选中、`new_game`、驱动 resolve、播 SFX/Music |
| `hud.gd` | 显示与按钮；`start_game` / `back_to_title` 信号（亮色样式在 `hud.tscn`） |
| `board.gd` | 网格数据、匹配、交换、限步开关、`ensure_no_initial_matches` / `ensure_has_moves`、`reset_game` / `clear_board` |
| `board_resolve.gd` | 消除（含连锁倍率）、下落填充、连锁循环 |
| `gem.gd` | 类型、SpriteFrames 动画、选中遮罩、交换/消除动画 |

### 8.3 状态机

```
WAITING →（开始）→ IDLE ⇄ SELECTED → SWAPPING → RESOLVING → IDLE
                                              ↘（限步用尽）→ GAME_OVER →（再来一局）→ …
IDLE → 点宝石 → SELECTED（可拖拽）
SELECTED → 再点已选中 → IDLE（取消）
SELECTED → 点相邻 / 拖过阈值 → SWAPPING
WAITING / SWAPPING / RESOLVING / GAME_OVER：锁棋盘输入
```

### 8.4 核心玩法参数（便于课件「作业改数值」）

| 常量/变量 | 含义 | 典型值 |
|-----------|------|--------|
| `GRID_SIZE` | 边长 | 8 |
| `CELL_SIZE` | 格像素 | 64 |
| `MOVES_DEFAULT` | 限步局步数 | 20 |
| `moves_limited` | 是否限步 | 默认 false |
| 基础分 | 3 连 / 每多 1 | 30 / +10 |
| 连锁 | 第 n 波 | ×n |
| `DRAG_SWAP_THRESHOLD` | 拖拽触发 | 28 px |
| `FALL_PER_CELL` | 下落单格时长 | 0.08 s |
| 交换/消除 Tween | 时长 | ~0.15 / ~0.2 s |

---

## 9. 与官方教程的对照（课件调整建议）

| 官方「第一个 2D 游戏」 | 本三消项目 | 建议课时落点 |
|------------------------|------------|--------------|
| 节点与场景树 | Main / Board / Gem / HUD | 第 1 课 |
| 信号（`hit` / `start_game`） | `score_changed`、`start_game`、`match_cleared` | 第 2 / 4 课 |
| 实例化敌人 | `gem.tscn` 实例化 | 第 1 课重点 |
| Timer 驱动 | Tween + `await` 驱动动画管道 | 第 3 课 |
| `$Music` / `$DeathSound` | `$Music` / `$Sfx*` | 第 4 课 |
| 独立 HUD 场景 | `hud.tscn` | 第 4 课 |
| 导出 | Windows + Web 预设 | 第 4 课 |

**差异（讲课时说明即可）：** 三消核心是二维数组与匹配算法，官方课是闪避与碰撞；本课用状态机锁输入，对应「为什么动画时不能乱点」。

---

## 10. 已知限制与后续可做

### 10.1 已知限制

- 无特殊宝石（炸弹/彩虹/四连造型）玩法  
- 无关卡目标分（仅限步可选结算）  
- 无消除提示 / 存档 / 多模式（属 M5；本教学版明确不做）  
- `board.gd` / `main.gd` 仍可能超过「150 行」教学软限制（功能完整优先；可再拆 `input` 脚本）  
- 拖拽与点击两套逻辑集中在 `main.gd`，零基础跟读略长（v2.1 已用规则表注释降低认知负担）  

### 10.2 适合作业 / Gamejam 扩展

- 改网格大小、颜色数、匹配长度  
- 连锁倍率公式、目标分数通关  
- 四连/五连特殊宝石  
- 换 BGM / 自做音效  
- Web 发布到 itch.io  

---

## 11. 课件大纲修订建议（摘要）

1. **第 1 课：** 强调 `gem.tscn` 实例化 + ColorRect/`AnimatedSprite2D` 二选一叙事（课上可用占位，成品再换贴图）。  
2. **第 2 课：** 写清「无效交换不扣步」；补充 `absi` 与警告当错误。选中用遮罩而非缩放。  
3. **第 3 课：** 明确拆 `board_resolve.gd` 的动机；动画串行；限步可演示但注明「成品为可选」。  
4. **第 4 课：** 以官方 HUD+Music 结构为准，不要再推「程序化 AudioStreamGenerator 为主」；标题半遮罩 + **开始后再生成棋盘** 写入验收。  
5. **打磨选修：** 拖拽交换阈值、背景配色（避开宝石色）。  
6. **v2.1 已入主线：** 开局 `ensure_has_moves`、再点取消选中、HUD 亮色 StyleBox、拖拽正式交互 —— 见第 7.5 节与课件大纲 v3。  

---

## 12. 关键文件索引（成品）

```
godot-match3 BasicMode/
├── project.godot
├── export_presets.cfg
├── README.md
├── 开发记录.md                 ← 本文件
├── 技术骨架.md / 课件大纲-4课时.md / 官方教程对照.md
├── cursor-prompts/
│   ├── M1~M4.md, FULL-完整成品.md, M5-后续优化.md（本仓库不执行）
│   └── 归档/BASIC-v2.1-体验下沉补丁.md   ← v2.1 已落地并归档
├── scenes/main.tscn, hud.tscn, gem.tscn
├── scripts/main.gd, hud.gd, board.gd, board_resolve.gd, gem.gd
└── assets/…（sprites / ui-kenney / bgm-kenney / 素材清单.md）
```

### 运行

```powershell
& "E:\Steam\steamapps\common\Godot Engine\godot.windows.opt.tools.64.exe" --path "D:\Godot Projects\godot-match3 BasicMode"
```

---

## 13. 版本备忘

| 项 | 值 |
|----|-----|
| 引擎 | Godot 4.7.1.stable（Steam） |
| 主场景 | `res://scenes/main.tscn` |
| 窗口 | 640×720 |
| 渲染 | gl_compatibility |
| 教学成品基线 | M1~M4 + 打磨 + **v2.1 体验下沉** |
| 代码协议建议 | MIT |
| 素材协议 | CC0（见素材清单 Credits） |

---

*本文档根据 2026-08-12 开发对话与仓库现状整理，供课件迭代使用。v2.1 体验下沉补丁已于同日落地并归档 Prompt。若实现与文档冲突，以当前仓库代码为准，并请回写本节。*
